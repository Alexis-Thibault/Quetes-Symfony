<h1>{{actor.name}}</h1>

		<strong>Série associés :</strong>
		<ul>
			{% for program in actor.programs %}
				<li>{{ program.title }}</li>
			{% else %}
				<li>Aucune série pour cet acteur</li>
			{% endfor %}
		</ul>